# IRCbot
A simple IRC bot that posts a notice when talked to

## How to use:
Simply run the following command to activate the bot (the default connection is to the [Libera Chat](http://libera.chat/)) network at the **#gromacs** channel).

`python3 IRCbot2.py`
